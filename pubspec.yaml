name: random_user_data_persistence
description: "using random user API for data persistence"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  drift: ^2.29.0
  drift_flutter: ^0.2.7
  dio: ^5.9.0
  go_router: ^16.2.5
  get_it: ^8.2.0
  result_dart: ^2.1.1
  pretty_dio_logger: ^1.4.0
  flutter_svg: ^2.2.1
  path_provider: ^2.1.5


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  drift_dev: ^2.29.0
  build_runner: ^2.9.0

flutter:

  uses-material-design: true

  assets:
    - lib/core/ui/assets/svg/
